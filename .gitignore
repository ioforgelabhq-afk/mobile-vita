# dependencies
node_modules/

# expo
.expo/
dist/
web-build/
expo-env.d.ts

# native (regenerated by `expo prebuild` — never hand-edit or commit)
/ios
/android
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# icon/splash source bundle (raw generator output — kept locally only)
/resources

# metro
.metro-health-check*

# secrets / env
.env*
!.env.example

# os / editor
.DS_Store
Thumbs.db
*.log
coverage/
